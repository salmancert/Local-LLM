flask
PyMuPDF
requests
chromadb
openai-whisper
pyttsx3
waitress
openai
# Pinned: 1.0.0+ replaced this thin OpenAI-compatible REST client with an
# unrelated in-process native-binding API (no .endpoint/.api_key).
foundry-local-sdk==0.5.1
sentence-transformers

# Optional, heavier dependency for natural-sounding local TTS (used
# automatically when installed -- see README). Also requires the
# espeak-ng system package.
# kokoro>=0.9.4
# soundfile
